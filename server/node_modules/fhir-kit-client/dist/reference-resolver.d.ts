import type { FhirResource, RequestOptions } from './types.js';
/**
 * Resolves FHIR References. Used internally by Client.
 */
export declare class ReferenceResolver {
    private readonly client;
    constructor(client: ReferenceResolver['client']);
    resolve({ reference, context, options, }: {
        reference: string;
        context?: FhirResource;
        options?: RequestOptions;
    }): Promise<FhirResource>;
    private resolveAbsoluteReference;
    private resolveContainedReference;
    private resolveBundleReference;
}
//# sourceMappingURL=reference-resolver.d.ts.map